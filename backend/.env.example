# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
# - For local development: http://localhost:3000
# - For Docker: http://localhost (overridden in docker-compose.yml)
CORS_ORIGIN=http://localhost:3000

# Database Connection (PostgreSQL with SSL for AWS RDS)
DATABASE_URL=postgresql://user:password@localhost:5432/maqai_db?sslmode=prefer

# Chatbot AI Fallback (optional - for text-input queries)
AI_PROVIDER=openai
AI_API_KEY=your_openai_api_key_here
